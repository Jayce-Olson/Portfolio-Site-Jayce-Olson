import { LetterWiggleDirective } from './letter-wiggle.directive';

describe('LetterWiggleDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterWiggleDirective();
    expect(directive).toBeTruthy();
  });
});
